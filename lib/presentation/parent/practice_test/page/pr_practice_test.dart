import 'package:flutter/material.dart';

class PrPracticeTestPage extends StatelessWidget {
  const PrPracticeTestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}