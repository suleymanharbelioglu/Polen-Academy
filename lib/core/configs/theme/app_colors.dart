import 'dart:ui';

class AppColors {
  static const primary = Color(0xff8E6CEF);
  static const secondBackground = Color(0xff1D182A);
  static const background = Color(0xff342F3F);

  /// Koçlar için mor
  static const primaryCoach = Color(0xff8E6CEF);

  /// Öğrenciler için kırmızı
  static const primaryStudent = Color(0xffE53935);

  /// Veliler için sarı
  static const primaryParent = Color(0xffFBC02D);

  /// Kart / yüzey vurgusu
  static const surface = Color(0xff2A2535);
  static const surfaceLight = Color(0xff3D3648);

  /// Kenarlık ve ayırıcı
  static const border = Color(0xff3D3648);
  static const borderLight = Color(0xff4A4355);

  /// Metin
  static const onSurface = Color(0xffE8E6EB);
  static const onSurfaceVariant = Color(0xffA8A4AE);
}
